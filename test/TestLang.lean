import Clingo
open Lean

open Clingo.Lang

def main : IO Unit := do
   -- println! "example: {clingo_term!(1)}"
   -- println! "example: {clingo_term!(- x)}"
   -- println! "example: {clingo_term!(f(p(x), q(x), d(x)))}"
   println! "language!!"

